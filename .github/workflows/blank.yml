name: CI


on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
env:
  # 使用配置变量的值设置环境变量
  env_var: ${{ vars.MY_VARIABLE }}  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo ${{ secrets.NPM_TOKEN }}
          echo "shell环境中的变量 : $env_var"
          echo "直接输出变量 : ${{ vars.MY_VARIABLE }}"
          echo "环境密钥 : ${{ secrets.NPM_TOKEN2 }}"
          echo "环境密钥 : ${{ env.NPM_TOKEN2 }}"
